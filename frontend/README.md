# HirePilot Frontend

Production SaaS frontend foundation for HirePilot, an AI-powered hiring platform.

## Run

```bash
npm install
npm run dev
```

The app reads the API gateway URL from `VITE_API_BASE_URL`.
